div.profile-container.mr-3.d-flex.flex-column.bg-white
  form.d-flex.flex-row.justify-content-center.align-items-center(action="/users/update/image" method="post" enctype="multipart/data-form")
    input.d-none(type="file", name="avatar")
    img.img-fluid(src="/images/avatardefault.svg")
  div.d-flex.flex-row.justify-content-center.align-items-center.my-3
    p=`${currentUser.username} (${currentUser.local.email})`
  div.d-flex.flex-row.justify-content-center.align-items-center.my-3.text-center
    div.flex-fill
      p
        strong Tweet
        br
        span.text-primary= tweets.length
    div.flex-fill
      p
        strong Following
        br
        span.text-primary -